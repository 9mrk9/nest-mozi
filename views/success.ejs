<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
    <title>Sikeres foglalás</title>
  </head>
  <body class="d-flex min-vh-100 justify-content-center align-items-center text-center flex-column gap-4 p-4">
    <% if(typeof rewritePath !== 'undefined') { %>
      <script>
        window.history.replaceState({}, '', '<%= rewritePath %>');
      </script>
    <% } %>
    <h1 class="text-success fw-bold">Sikeres foglalás</h1>
    <div class="card p-5 shadow-lg">
      <h3 class="mb-4">Foglalási adatok</h3>
      <p><strong>Név:</strong> <%= booking.name %></p>
      <p><strong>E-mail cím:</strong> <%= booking.email %></p>
      <p>
        <strong>Időpont:</strong> <%= new Intl.DateTimeFormat('hu-HU', {
          year: 'numeric',
          month: 'long',
          day: 'numeric',
          hour: '2-digit',
          minute: '2-digit',
        }).format(booking.date) %>
      </p>
      <p><strong>Foglalt székek:</strong> <%= booking.peopleCount %> db</p>
    </div>
    <a href="/" class="btn btn-success px-4 py-2">Új foglalás</a>
  </body>
</html>
